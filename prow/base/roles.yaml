---
kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: "plank-pods"
rules:
  - apiGroups:
      - ""
    resources:
      - pods
    verbs:
      - create
      - delete
      - list

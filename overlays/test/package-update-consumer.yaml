---
apiVersion: apps.openshift.io/v1
kind: DeploymentConfig
metadata:
  labels:
    app: thoth
    component: package-update-consumer
  name: package-update-consumer
spec:
  template:
    spec:
      containers:
        - name: package-update-consumer
          image: "image-registry.openshift-image-registry.svc:5000/thoth-goern-test/package-update-job:latest"

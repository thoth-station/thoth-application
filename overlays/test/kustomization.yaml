apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - github.com/goern/thoth-application?ref=master
  - postgresql-ephemeral.yaml
patchesStrategicMerge:
  - configmaps.yaml
  - imagestreamtags.yaml
  - init-job.yaml
  - package-update-consumer.yaml
generatorOptions:
  disableNameSuffixHash: true

---
kind: Service
apiVersion: v1
metadata:
  name: qeb-hwt-webhook-receiver
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    deploymentconfig: qeb-hwt-webhook-receiver

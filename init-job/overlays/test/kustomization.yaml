apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app.kubernetes.io/version: "0.6"
resources:
  - ../../base
  - thoth-notification.yaml
images:
  - name: init-job
    newName: quay.io/thoth-station/init-job
    newTag: v0.6.0-dev
patchesStrategicMerge:
  - imagestreamtag.yaml
patchesJson6902:
  - path: job-generate-name.yaml
    target:
      group: batch
      version: v1
      kind: Job
      name: chat-notification-

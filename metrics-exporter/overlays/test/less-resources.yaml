---
---
kind: DeploymentConfig
apiVersion: apps.openshift.io/v1
metadata:
  name: metrics-exporter
spec:
  template:
    spec:
      containers:
        - name: metrics-exporter
          resources:
            requests:
              cpu: "1"
            limits:
              cpu: "1"
